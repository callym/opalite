[package]
name = "opalite_macro"
version = "0.1.0"
authors = ["Callym <hi@callym.com>"]

[lib]
proc-macro = true

[features]
internal = []

[dependencies]
syn = { version = "0.12", features = ["full"] }
quote = "0.4"
proc-macro2 = { version = "0.2", features = ["nightly"] }
